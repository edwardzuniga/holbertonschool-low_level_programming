#include "main.h"

/**
* is_palindrome - returns 1 if a string is a palindrome and 0 if not
* @s: numero string
* Return: value 0 or 1
*/

int is_palindrome(char *s)
{
int a;

a = 0;

if (*s == a)
return (0);
return (1);
}
